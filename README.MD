# PDF Chatbot
Chat with any PDF using Gemini AI with voice responses via ElevenLabs.

## Setup
1. Clone the repo
2. pip install -r requirements.txt
3. Create a .env file with GEMINI_API_KEY and API_VOICE
4. python -m streamlit run Main.py